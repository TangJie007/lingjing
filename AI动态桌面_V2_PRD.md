# 🎨 灵境（LingScape）V2.0 — 产品需求文档

> **文档状态**：V2.0 完整版（含完整分区功能）
> **版本**：v2.0
> **日期**：2026-06-14
> **公司**：灵构科技（LingGou Tech）
> **作者**：产品通
> **依赖**：本文档基于 V1.3 PRD，请先阅读 V1 文档了解基础架构

---

## 📌 V2.0 版本定位

> **V1 验证了"能用"，V2 要做到"好用"和"好看"。**

V1 解决了核心问题：AI 能生成壁纸、壁纸能播放、用户能上手。  
V2 的任务是：**提升浏览体验、打磨细节交互、补齐留存功能、新增完整桌面整理**。

| 版本 | 定位 | 核心主题 |
|------|------|---------|
| V1.0 | 能用 | AI 壁纸生成 + BYOK 打通 |
| **V2.0** | **好用 + 好看** | **壁纸可视化浏览 + 退出恢复 + 完整桌面分区整理 + V1 体验补全** |
| V3.0 | 生态 | UGC 社区 + 多平台 + 音乐联动 |

---

## 1. 需求概要

| 字段 | 内容 |
|------|------|
| **产品名称** | 灵境（LingScape） |
| **版本** | V2.0 |
| **目标平台** | Windows 10 (21H2+) / Windows 11 |
| **前置条件** | 用户已完成 V1 安装，配置好火山引擎 API Key |
| **V2 定价策略** | V1 用户免费升级；Steam 新用户仍 ¥9.9 买断 |
| **预期发布** | V1 上线后 3-4 个月 |

---

## 2. V2 需求来源分析

### 2.1 来自用户明确诉求（本次需求单）

| # | 用户诉求 | 功能模块 | 优先级 |
|---|---------|---------|:--:|
| R-01 | 壁纸选择卡片要有缩略图 | 壁纸库视觉化 | **P0** |
| R-02 | 动态壁纸等所有类型都需要图片展示 | 壁纸库视觉化 | **P0** |
| R-03 | 支持点进去查看壁纸详情 | 壁纸详情页 | **P0** |
| R-04 | 导入壁纸按导入时间降序排列 | 壁纸排序规则 | **P0** |
| R-05 | 退出应用后恢复原始桌面 | 退出恢复机制 | **P0** |
| R-06 | 提供完整桌面分区整理功能 | 桌面整理模块 | **P0** |

### 2.2 来自 V1 遗漏补充（系统梳理）

| # | V1 遗漏项 | 补充原因 | 优先级 |
|---|---------|---------|:--:|
| R-07 | 壁纸自动定时轮换 | V1.5 计划提前至 V2 | P1 |
| R-08 | 多显示器独立壁纸 | 用户呼声高，双屏用户比例约 30% | P1 |
| R-09 | 壁纸收藏/标签分类 | 壁纸库日益增大，需要分类管理 | P1 |
| R-10 | AI 生成历史重新生成 | 满意某次 prompt，想微调重新生成 | P1 |
| R-11 | 壁纸导出/备份 | 用户换电脑后希望带走壁纸库 | P2 |
| R-12 | 多平台 API 支持 | V1 仅火山引擎，V2 加 DeepSeek + 可灵 | P1 |
| R-13 | 生成进度实时反馈 | V1 只有转圈动画，用户不知道进度 | P1 |
| R-14 | 壁纸播放速度/亮度/饱和度调节 | 部分用户反映默认效果不满意 | P2 |
| R-15 | 快捷键支持 | 进阶用户需求：快速切换/暂停 | P2 |
| R-16 | 应用内更新检测 | 避免用户手动重下 | P0 |

---

## 3. 成功指标

| 指标 | V1 目标 | V2 目标 | 测量方式 |
|------|--------|--------|---------|
| Steam 好评率 | ≥ 80% | ≥ 88% | Steam 评价 |
| 30 日留存 | ≥ 50% | ≥ 65% | 应用内埋点 |
| 壁纸库浏览到应用转化率 | 无 | ≥ 40% | 埋点漏斗 |
| 桌面整理使用率（7日内） | — | ≥ 25% | 功能使用埋点 |
| 分区功能使用率（7日内） | — | ≥ 15% | 分区操作埋点 |
| 退出恢复投诉率 | — | ≈ 0% | Steam 退款/差评关键词 |
| 多平台 API 配置率 | 火山引擎 | 新增 DeepSeek / 可灵配置率 ≥ 15% | API 配置埋点 |

---

## 4. 功能详细说明

---

### F-01：壁纸库视觉化升级（P0）— 对应 R-01、R-02

#### 4.1.1 功能概述

> V1 壁纸库以列表/文字为主，缺乏直观预览。V2 全面升级为**卡片缩略图网格模式**，所有类型壁纸（静态图片、动态视频、AI 生成）都必须有图片展示。

#### 4.1.2 卡片设计规格

```
┌──────────────────────────────────────────────────┐
│  壁纸库                              [网格] [列表] [筛选▼] [排序▼] │
│                                                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │          │  │  ▶ GIF   │  │  🎬 MP4  │         │
│  │ [缩略图]  │  │ [缩略图]  │  │ [缩略图]  │         │
│  │          │  │          │  │          │         │
│  ├──────────┤  ├──────────┤  ├──────────┤         │
│  │ 水墨山水  │  │ 赛博朋克  │  │ 星空延时  │         │
│  │ AI生成   │  │ AI生成   │  │ 本地导入  │         │
│  │ ♥  ···  │  │ ♥  ···  │  │ ♥  ···  │         │
│  └──────────┘  └──────────┘  └──────────┘         │
│                                                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │          │  │          │  │    +     │         │
│  │ [缩略图]  │  │ [缩略图]  │  │  导入壁纸 │         │
│  │          │  │          │  │          │         │
│  ├──────────┤  ├──────────┤  └──────────┘         │
│  │ 极简渐变  │  │ 深海蓝鲸  │                        │
│  │ 本地导入  │  │ AI生成   │                        │
│  │ ♥  ···  │  │ ♥  ···  │                        │
│  └──────────┘  └──────────┘                        │
└──────────────────────────────────────────────────┘
```

#### 4.1.3 卡片规格

| 元素 | 规格 | 说明 |
|------|------|------|
| **卡片尺寸** | 180×140px（可调，支持大/中/小三档） | 默认中号 |
| **缩略图比例** | 16:9 | 对应桌面比例 |
| **动态壁纸标识** | 右上角角标：`▶ GIF` / `🎬 MP4` / `🎬 WebM` | 一眼区分类型 |
| **AI 生成标识** | 左下角标：`✨ AI` | 区分来源 |
| **当前应用中** | 卡片边框高亮 + 左上角 `● 当前` 标签 | 告知用户当前壁纸 |
| **动态预览** | hover 停留 0.5s 后，GIF/MP4 自动播放预览（静音） | 直观感受动态效果 |
| **收藏按钮** | `♥` 图标，点击切换收藏状态 | 快速收藏 |
| **更多操作** | `···` 展开菜单：设为壁纸 / 查看详情 / 收藏 / 导出 / 删除 | 操作集中 |

#### 4.1.4 动态壁纸缩略图生成策略

| 类型 | 缩略图方案 | 时机 |
|------|-----------|------|
| **MP4/WebM** | 截取第 0.5 秒帧作为封面图 | 导入/生成时自动生成，本地缓存 |
| **GIF** | 截取第一帧 | 导入时自动生成 |
| **AI 生成图片** | 直接缩放 | 生成完成即可显示 |
| **AI 生成视频** | 截取第 0.5 秒帧 | 即梦视频下载后自动提取 |

> **技术要求**：缩略图生成在后台线程完成，不阻塞 UI。缩略图以 WebP 格式缓存，文件名为原文件 hash + `.thumb.webp`。

#### 4.1.5 排序规则（对应 R-04）

| 排序方式 | 规则 | 默认 |
|---------|------|:--:|
| **导入时间（降序）** | 最新导入/生成的在最前面 | ✅ 默认 |
| 导入时间（升序） | 最早的在最前 | — |
| 名称 A→Z | 按文件名 / AI 描述字母序 | — |
| 文件大小（大→小） | 视频 > 图片 | — |
| 最近使用 | 最近设为桌面的在最前 | — |

> **排序规则说明**：导入壁纸和 AI 生成壁纸使用同一排序字段（入库时间）。"AI 生成"的入库时间 = 生成完成时间。

---

### F-02：壁纸详情页（P0）— 对应 R-03

#### 4.2.1 功能概述

点击任意壁纸卡片 → 进入全屏或侧边栏详情页，展示大图预览 + 完整元数据 + 操作按钮。

#### 4.2.2 详情页布局

```
┌───────────────────────────────────────────────────────────────┐
│  ← 返回壁纸库          灵境 — 壁纸详情            [ × 关闭 ]      │
├────────────────────────────┬──────────────────────────────────┤
│                            │  📋 壁纸信息                       │
│                            │  ─────────────────────────────── │
│    [壁纸大图预览]            │  名称：赛博朋克夜晚雨巷              │
│    （16:9 比例，填满左侧）   │  类型：AI 生成 · 静态图片            │
│                            │  分辨率：1920 × 1080              │
│    [动态壁纸：有播放控制栏]  │  文件大小：3.2 MB                  │
│    [  ◀  ▌▌  ▶  ]        │  导入时间：2026-06-14 09:30        │
│                            │  AI 模型：Seedream 4.0            │
│                            │                                   │
│                            │  🧠 AI 构建方案                    │
│                            │  ─────────────────────────────── │
│                            │  原始描述：赛博朋克雨天小巷           │
│                            │  优化 Prompt：A rainy cyberpunk   │
│                            │  alley at night, neon lights      │
│                            │  reflecting on wet pavement...    │
│                            │  风格标签：赛博朋克 · 夜景 · 雨天     │
│                            │  色调：深蓝 · 紫 · 霓虹橙            │
│                            │  [复制 Prompt]  [用此方案重新生成]  │
│                            │                                   │
│                            │  ─────────────────────────────── │
│                            │  [ ✨ 设为桌面壁纸 ]  ( 当前应用中 ) │
│                            │  [ ♥ 收藏 ]  [ 📤 导出 ]  [ 🗑 删除 ] │
└────────────────────────────┴──────────────────────────────────┘
```

#### 4.2.3 详情页字段说明

| 字段 | AI 生成壁纸 | 本地导入壁纸 |
|------|:--:|:--:|
| 名称 | AI 描述前 20 字 | 文件名 |
| 类型 | AI 生成 · 静态/动态 | 本地导入 · 静态/动态 |
| 分辨率 | ✅ | ✅ |
| 文件大小 | ✅ | ✅ |
| 导入/生成时间 | 生成完成时间 | 文件导入时间 |
| AI 模型 | Seedream 4.0 等 | — |
| 原始描述 | ✅ | — |
| 优化 Prompt | ✅ | — |
| 风格标签 | ✅ | — |
| 色调预览 | ✅（3 个色块） | — |
| 复制 Prompt | ✅ | — |
| 用此方案重新生成 | ✅ | — |

#### 4.2.4 动态壁纸大图预览

- MP4/GIF/WebM 在详情页自动播放（有静音控制），无限循环
- 视频有播放进度条 + 暂停/播放控制
- 支持键盘快捷键：空格 = 暂停/播放，左右箭头 = 上/下一张壁纸（列表导航）

---

### F-03：退出应用恢复原始桌面（P0）— 对应 R-05

#### 4.3.1 功能概述

> 当用户关闭/退出灵境时，自动将 Windows 桌面壁纸恢复为**进入灵境之前的原始壁纸**，不留下任何残留。

#### 4.3.2 行为规则

| 场景 | 行为 |
|------|------|
| **正常退出**（点击退出按钮） | 播放停止 → 恢复原始壁纸 → 进程退出 |
| **关闭窗口（最小化到托盘）** | 壁纸继续播放，**不恢复**（用户只是收起了窗口） |
| **从托盘真正退出** | 播放停止 → 恢复原始壁纸 → 进程退出 |
| **应用崩溃** | 崩溃恢复机制：下次启动时检测是否有未恢复的原始壁纸，若有则询问用户是否恢复 |
| **系统关机/重启** | Windows 会话结束时触发退出事件，尝试恢复原始壁纸 |

#### 4.3.3 原始壁纸快照策略

```
启动灵境时：
  1. 读取 Windows 当前壁纸路径（RegQuery HKCU\Control Panel\Desktop\Wallpaper）
  2. 复制当前壁纸文件 → 保存到 %AppData%\LingScape\backup\original_wallpaper.*
  3. 保存壁纸显示方式（填充/适应/拉伸等）
  4. 标记「有原始壁纸快照」

退出灵境时：
  1. 检测快照存在
  2. 调用 SystemParametersInfo(SPI_SETDESKWALLPAPER) 恢复
  3. 恢复壁纸显示方式
  4. 删除快照文件
  5. 清除「有原始壁纸快照」标记
```

#### 4.3.4 设置选项

| 设置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| 退出时恢复原始桌面 | 开关 | **开启** | 用户可关闭（如希望保留灵境壁纸） |
| 询问退出时是否恢复 | 开关 | 关闭 | 开启后每次退出弹出确认对话框 |

#### 4.3.5 验收标准

| Given | When | Then |
|-------|------|-------|
| 用户桌面原为 Windows 自带壁纸，打开灵境并应用 AI 壁纸 | 从系统托盘选择「退出灵境」 | 桌面恢复为 Windows 自带壁纸，灵境进程完全结束 |
| 灵境崩溃未正常退出 | 重新打开灵境 | 启动时检测到未恢复快照，弹出提示「上次异常退出，是否恢复原始桌面壁纸？[是][否]」 |

---

### F-04：桌面整理功能（P0）— 对应 R-06 完整分区版

#### 4.4.1 功能定位与设计原则

> 桌面整理模块使灵境从"壁纸工具"升级为"完整桌面美化套件"，与壁纸形成产品闭环。对标 Fences 核心能力，但在技术路线上选择更安全的「视觉覆盖式」实现。

**设计原则**：

| 原则 | 说明 |
|------|------|
| 不破坏用户文件 | 分区只是"视觉容器 + 位置记录"，图标文件不动，删除分区不删文件 |
| 兼容 Explorer | 分区层是透明覆盖窗口（Always-on-bottom），不替换系统桌面，Explorer 重启后可恢复 |
| 渐进增强 | 基础功能（隐藏图标 / 分区创建）零配置可用；高级功能（文件夹门户 / 自动规则）按需开启 |
| Fences 对标 | 核心交互参照 Fences，但在视觉风格上形成灵境自己的设计语言 |

---

#### 4.4.2 功能总览（完整版）

| 功能模块 | 功能点 | 优先级 | 参照 |
|---------|--------|:--:|------|
| **基础控制** | 一键隐藏/显示所有桌面图标 | **P0** | Fences 双击桌面 |
| **基础控制** | 一键对齐网格 / 按名称·类型·大小·日期排序 | **P0** | Windows 原生增强 |
| **分区核心** | 鼠标拖拽创建分区（栅栏区域） | **P0** | Fences 核心 |
| **分区核心** | 分区命名 / 调色 / 透明度调节 | **P0** | Fences |
| **分区核心** | 卷起/展开（双击分区标题栏） | **P0** | Fences 标志性交互 |
| **分区核心** | 图标拖入分区 / 分区内拖拽排序 | **P0** | Fences |
| **分区核心** | 分区大小调节 / 位置移动 | **P0** | — |
| **分区高级** | 文件夹门户（将真实文件夹映射为分区） | **P1** | Fences 文件夹门户 |
| **分区高级** | Peek 功能（全局热键临时呼出分区，全屏应用时可用） | **P1** | Fences Peek |
| **分区高级** | 自动整理规则（按文件类型/后缀自动归入指定分区） | **P1** | Fences 自动化 |
| **分区高级** | 多显示器分区独立布局 | **P1** | Fences 多屏 |
| **布局管理** | 分区布局导入/导出/重置 | **P1** | — |
| **布局管理** | 启动时自动应用上次分区布局 | **P0** | — |

---

#### 4.4.3 核心交互设计

**① 进入整理模式**

```
用户点击侧边栏「桌面整理」→ 进入整理模式

整理模式两种触发方式：
  [方式 A] 常驻模式：侧边栏切换至「桌面整理」页，桌面进入可编辑状态（推荐）
  [方式 B] 覆盖模式：点击「整理桌面」按钮，桌面短暂进入编辑状态，
               完成后退出编辑，分区常驻显示

推荐：方式 A（常驻模式），用户可随时开关
```

**② 分区创建**

```
在整理模式下：
  1. 点击「+ 新建分区」
  2. 鼠标变为十字光标，拖拽绘制矩形区域
  3. 释放鼠标 → 弹出「命名分区」输入框
  4. 输入名称（如"工作区"、"娱乐"、"临时文件"）
  5. 分区创建完成，出现透明分区容器

快捷操作：
  - 从「推荐分区模板」一键创建：["工作区""娱乐""工具""临时"]
  - 右键桌面空白处 → 「新建分区」
```

**③ 分区视觉规格**

```
分区容器（透明窗口）视觉构成：

  ┌────────────────────────┐  ← 分区标题栏（28px 高）
  │ 📁 工作区            ⋯ │  ← 图标 + 名称 + 设置菜单
  ├────────────────────────┤
  │                        │
  │   [图标] [图标] [图标] │  ← 图标网格（自动排列）
  │   [图标] [图标]       │
  │                        │
  │                        │
  └────────────────────────┘  ← 分区可拖拽缩放

默认样式：
  - 背景：半透明深色（#000000，20% 不透明度）
  - 标题栏：半透明深色（#000000，40% 不透明度）
  - 边框：1px 圆角矩形，颜色可自定义
  - 字体：系统默认，白色，12px
  - 图标网格：自适应排列，间距 8px
```

**④ 卷起/展开（Fences 标志性交互）**

```
双击分区标题栏：
  → 卷起状态：只显示标题栏，图标区域折叠
  → 再次双击：展开状态，图标区域恢复

卷起状态视觉：
  ┌────────────────────────┐
  │ 📁 工作区            ⋯ │  ← 只显示这一行
  └────────────────────────┘

设置项：
  □ 启动灵境时自动卷起所有分区（默认关闭）
  □ 双击桌面空白处切换所有分区显示/隐藏（默认关闭，避免和系统冲突）
```

**⑤ 图标操作**

| 操作 | 行为 |
|------|------|
| 拖桌面图标入分区 | 图标「进入」分区，分区内自动网格对齐；**文件实际位置不变**，灵境记录「该图标属于该分区」 |
| 分区内拖拽图标 | 调整图标在分区内的位置，记录新位置 |
| 从分区拖出图标 | 图标移回桌面自由区，从分区记录中移除 |
| 分区内右键图标 | 系统原生右键菜单（打开/删除/属性等） |
| 分区标题栏右键 | 分区设置菜单：重命名 / 变色 / 透明度 / 删除分区 |
| 选中多个图标 | 分区内支持框选，可批量移入/移出分区 |

---

#### 4.4.4 文件夹门户（P1 高级功能）

> Fences 的「文件夹门户」是其最受欢迎的高级功能之一：将真实文件夹映射到桌面分区，直接在桌面操作文件夹内容，无需打开资源管理器。

**功能设计**：

```
创建文件夹门户：
  1. 新建分区 → 选择「文件夹门户」模式（替代普通分区）
  2. 选择本地文件夹（如 D:\Projects、下载、桌面\截图）
  3. 分区创建完成，标题栏显示文件夹图标 + 文件夹名称
  4. 分区内显示该文件夹内的所有文件和子文件夹（实时同步）

交互特性：
  - 双击分区内文件 → 用系统默认程序打开
  - 拖入新文件到分区 → 实际移动到映射的文件夹
  - 文件夹内容变化（外部修改）→ 分区内实时刷新
  - 标题栏右侧显示「在资源管理器中打开」按钮
```

---

#### 4.4.5 Peek 功能（P1 高级功能）

> 用户在全屏应用（游戏、PPT、全屏视频）时，按热键临时呼出所有分区，快速找到/打开文件，然后自动消失。

**功能设计**：

```
默认热键：Win + Shift + P  （用户可自定义）

行为：
  1. 用户按 Win+Shift+P
  2. 所有分区（包括卷起状态的）临时显示在最前端（覆盖全屏应用）
  3. 显示时长：无操作 3 秒后自动隐藏；有操作时持续显示
  4. 点击分区内文件 → 打开文件，分区自动隐藏
  5. 按 Esc → 立即隐藏分区

设置项：
  □ 启用 Peek 功能（默认开启）
  □ 自定义 Peek 热键（默认 Win+Shift+P）
  □ Peek 显示时长（3s / 5s / 10s / 手动关闭）
```

---

#### 4.4.6 自动整理规则（P1 高级功能）

> 用户可以设定规则，新创建的特定类型文件自动归入指定分区。

**功能设计**：

```
规则配置界面：

  ┌─────────────────────────────────────┐
  │  ⚙️ 自动整理规则                      │
  │                                     │
  │  [+] 新建规则                       │
  │                                     │
  │  ┌─────────────────────────────┐    │
  │  │ 规则 1                          │  │
  │  │ 当文件类型：☑️ .jpg ☑️ .png ☑️ .gif  │  │
  │  │ 或者包含关键词：「截图」          │  │
  │  │ 自动归入分区：[图片]            │  │
  │  │ ☑️ 启用    [删除规则]          │  │
  │  └─────────────────────────────┘    │
  │                                     │
  │  推荐规则模板：                      │
  │  📷 图片文件 → [图片] 分区        │
  │  📄 文档文件 → [文档] 分区        │
  │  🎮 游戏快捷方式 → [娱乐] 分区    │
  │  📥 下载文件 → [临时] 分区        │
  └─────────────────────────────────────┘

触发时机：
  - 实时监听：文件系统 Watcher 监听桌面文件变化
  - 手动触发：「立即整理桌面」按钮
```

---

#### 4.4.7 技术实现方案

**核心技术栈（参考 NoFences 开源方案）**：

| 技术点 | 方案 | 说明 |
|--------|------|------|
| **分区容器** | Win32 透明窗口（`WS_POPUP` + `WS_EX_LAYERED` + `WS_EX_TOOLWINDOW`） | 每个分区是一个独立透明窗口，Z-order 设置为桌面层级 |
| **桌面图标枚举** | `FindWindow("ProgMan")` → `FindWindowEx(WorkerW/SHELLDLL_DefView)` → `SysListView32` | 获取桌面所有图标的名称、位置、缩略图 |
| **图标位置控制** | `ListView_SetItemPosition`（需谨慎，部分 Windows 版本有限制） | 移动图标到分区内网格位置；更安全的方案是「记录位置，不实际移动图标」 |
| **图标位置记录** | 本地 JSON：`{iconPath, partitionId, gridX, gridY}` | 图标实际位置不动，灵境记录「逻辑归属」；渲染分区时，在图标上方绘制分区视觉边界 |
| **分区视觉渲染** | DirectX / GDI+ 透明窗口绘制 | 分区边框、背景、标题栏全部自绘 |
| **桌面快照兼容** | 监听 `Shell_TrayWnd` 消息 + Explorer 重启检测 | Explorer 重启后重建分区窗口 |
| **多显示器** | `EnumDisplayMonitors` 枚举显示器，每个显示器独立分区容器 | — |
| **性能保障** | 图标缩略图延迟加载 + 分区可见性裁剪 + 消息节流 | 100+ 图标时不卡顿 |

**两种实现路线对比**：

| 路线 | 方案 | 优点 | 缺点 | 推荐 |
|------|------|------|------|:--:|
| **A：视觉覆盖式** | 图标实际位置不动，分区是透明覆盖层，视觉上"看起来"图标在分区里 | 不破坏系统桌面，Explorer 重启后易恢复，兼容性好 | 图标实际上不在分区里，技术上是"视觉分组" | ⭐⭐⭐⭐⭐ |
| **B：真的移动图标** | 调用 `ListView_SetItemPosition` 真正把图标移到分区区域 | 和 Fences 完全一致，分区是"真的" | Windows 版本差异大，Explorer 更新后容易挂，风险高 | ⭐⭐ |

> **强烈推荐路线 A（视觉覆盖式）**：NoFences 也采用类似思路。核心原因是 Windows 11 23H2+ 对 SysListView32 的管控越来越严，直接操作图标位置在新版本 Windows 上兼容性风险高。

---

#### 4.4.8 设置项一览

| 设置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| 启用桌面整理 | 总开关 | 开启 | 关闭后分区全部隐藏，恢复原始桌面 |
| 启动灵境时恢复分区布局 | 开关 | 开启 | 记住上次分区状态 |
| 双击桌面空白处隐藏/显示分区 | 开关 | 关闭 | 避免和系统「排列图标」右键冲突 |
| 分区背景不透明度 | 滑块 | 20% | 0%=完全透明，100%=不透明 |
| 分区标题栏不透明度 | 滑块 | 40% | — |
| 分区圆角 | 开关+数值 | 开启，8px | — |
| Peek 热键 | 快捷键录入 | Win+Shift+P | — |
| 自动整理 | 开关 | 关闭 | 开启后按规则自动归类新文件 |
| 卷起状态下显示图标数量 | 开关 | 开启 | 标题栏显示「工作区 (12)」 |

---

#### 4.4.9 非目标（明确不做）

| 不做 | 原因 |
|------|------|
| 替换系统桌面（自己渲染图标） | 复杂度极高，和系统深度耦合，风险不可控 |
| 修改图标样式（自定义图标图片） | 需要 Shell 图标覆盖处理器，涉及系统稳定性 |
| 任务栏美化 | 独立功能模块，V3 评估 |
| 桌面小组件（天气/日历贴靠在分区上） | 范围蔓延，V3+ 考虑 |
| 分区内文件搜索 | 系统 Search 已覆盖，不重复实现 |

---

#### 4.4.10 验收标准

| Given | When | Then |
|-------|------|-------|
| 用户桌面有 20 个乱散图标 | 进入整理模式，创建 3 个分区，将图标拖入 | 分区视觉上包含图标，双击分区标题栏可卷起/展开 |
| 用户配置了文件夹门户 | 往映射文件夹放入新文件 | 分区内实时显示新文件，无需刷新 |
| 用户按 Peek 热键（全屏应用中） | 按下 Win+Shift+P | 所有分区临时覆盖显示，3 秒无操作自动消失 |
| 用户设置了自动整理规则（.jpg→图片分区） | 桌面出现新 .jpg 文件 | 文件自动出现在图片分区内（需系统通知或手动触发） |
| Explorer 重启（杀掉再开） | Explorer 重新加载 | 分区容器在 3 秒内自动重建，布局不变 |
| 用户删除分区 | 确认删除 | 分区消失，图标文件不受影响，仍保留在桌面 |

---

### F-05：壁纸收藏与标签分类（P1）— 对应 R-09

#### 4.5.1 功能概述

壁纸库随使用积累会越来越大，V2 新增收藏和标签分类能力，让用户快速找到想要的壁纸。

#### 4.5.2 功能设计

| 功能 | 说明 |
|------|------|
| **一键收藏** | 卡片上 ♥ 按钮，收藏后可在「收藏」筛选标签快速查看 |
| **自定义标签** | 用户可为壁纸添加 1-5 个标签（如"工作日"、"夜间"、"赛博"） |
| **系统自动标签** | AI 生成壁纸自动打上风格标签（来自构建方案的风格标签字段） |
| **标签筛选** | 壁纸库顶部可按标签过滤展示 |
| **快速搜索** | 壁纸库支持搜索框，按名称/标签/AI描述关键词搜索 |

```
壁纸库顶部筛选栏：

  [全部] [♥ 收藏] [✨ AI生成] [本地导入] [动态] | [赛博] [水墨] [极简] [工作日] [+]
                                                        ↑ 用户自定义 / AI 自动标签
```

---

### F-06：多平台 API 支持扩展（P1）— 对应 R-12

#### 4.6.1 新增支持的平台

| 平台 | 步骤 | 模型 | 定价 | 接入计划 |
|------|:--:|------|------|:--:|
| **DeepSeek** | 第一步 | DeepSeek-V4 | ¥2/M tokens | V2 首发 |
| **快手可灵** | 第二步（视频） | 可灵 2.6 Pro | $0.049/秒 | V2 首发 |
| 智谱 AI | 第一步 | GLM-4V-Plus | ¥5/M tokens | V2.1 |

#### 4.6.2 UI 变化

- API 管理页新增 DeepSeek 和 可灵 两张平台卡片
- 生成界面新增「生成模式」下拉：图片模式（Seedream）/ 视频模式（可灵 / 即梦）
- 用户可配置：第一步偏好模型 + 第二步偏好模型，系统记住上次选择

---

### F-07：壁纸自动定时轮换（P1）— 对应 R-07

#### 4.7.1 功能概述

用户可以创建「轮换列表」，系统按设定时间间隔自动切换壁纸。

| 设置项 | 选项 |
|--------|------|
| 轮换来源 | 全部壁纸 / 仅收藏 / 指定标签 / 手动选择列表 |
| 轮换间隔 | 15分钟 / 30分钟 / 1小时 / 每天 / 每次解锁屏幕 |
| 轮换顺序 | 顺序 / 随机 |
| 轮换时间段 | 全天 / 工作时间（9:00-18:00）/ 自定义 |

---

### F-08：多显示器独立壁纸（P1）— 对应 R-08

#### 4.8.1 功能概述

检测到多显示器时，支持为每块屏幕独立设置不同壁纸（静态或动态）。

#### 4.8.2 行为设计

```
设置 → 显示器配置：

  ┌─────────────────────────────────────────────────┐
  │  检测到 2 个显示器                                 │
  │                                                  │
  │  ┌──────────────┐    ┌──────────────┐            │
  │  │  显示器 1     │    │  显示器 2     │            │
  │  │  主屏 1920×1080│   │  副屏 2560×1440│          │
  │  │              │    │              │            │
  │  │ [当前壁纸预览] │    │ [当前壁纸预览] │            │
  │  │              │    │              │            │
  │  │ [更换壁纸]    │    │ [更换壁纸]    │            │
  │  └──────────────┘    └──────────────┘            │
  │                                                  │
  │  □ 所有显示器使用相同壁纸                           │
  └─────────────────────────────────────────────────┘
```

---

### F-09：AI 生成历史重新生成（P1）— 对应 R-10

#### 4.9.1 功能概述

在壁纸详情页（F-02）中，AI 生成的壁纸可以「用此方案重新生成」，支持微调后再次生成。

#### 4.9.2 操作流程

```
壁纸详情页 → 「用此方案重新生成」

  → 打开 AI 创作页面，预填：
    - 原始描述：赛博朋克夜晚雨巷
    - 优化 Prompt：A rainy cyberpunk alley...（可修改）
    - 风格标签：赛博朋克 · 夜景（可修改）
    - 模型：Seedream 4.0（可更换）
    - 数量：3 张（可调）

  → 用户确认 / 微调后点击生成 → 新一批变体生成
```

---

### F-10：生成进度实时反馈（P1）— 对应 R-13

#### 4.10.1 问题分析

V1 生成时只有旋转动画，用户不知道到底进行到哪一步，也不知道还要多久。V2 要做到清晰的分步进度反馈。

#### 4.10.2 进度展示设计

```
生成中状态：

  ┌─────────────────────────────────────────────────┐
  │  🧠 第一步：AI 正在理解你的意图...               ✅   │
  │     用时 2.1 秒                                   │
  │                                                  │
  │  🎨 第二步：Seedream 4.0 生成中...               ⏳   │
  │     ████████░░░░░░░░░░░░  40%                    │
  │     预计还需 8 秒                                  │
  │                                                  │
  │  [取消生成]                                        │
  └─────────────────────────────────────────────────┘
```

| 字段 | 说明 |
|------|------|
| 第一步状态 | 完成后显示 ✅ + 实际用时 |
| 第二步进度条 | 基于 API 轮询或预估时间（Seedream 通常 5-15 秒）模拟 |
| 预计剩余时间 | 根据历史平均时间估算，显示"预计还需 X 秒" |
| 取消按钮 | 允许用户在第二步生成中取消，不扣费（API 侧是否扣费取决于 Seedream 政策） |

---

### F-11：应用内更新检测（P0）— 对应 R-16

#### 4.11.1 功能概述

应用启动时检测是否有新版本，有则通知用户；对于 Steam 用户，跳转 Steam 更新即可。

| 渠道 | 更新方式 |
|------|---------|
| Steam 版 | 依赖 Steam 自动更新，启动时只做版本号展示 |
| 官网直售版 | 启动后台请求版本接口（GitHub Release / 自建 JSON）→ 有新版弹横幅提示 + 下载链接 |

---

### F-12：快捷键支持（P2）— 对应 R-15

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+Shift+W` | 全局：随机切换下一张壁纸 |
| `Ctrl+Shift+P` | 全局：暂停/继续动态壁纸播放 |
| `Ctrl+Shift+H` | 全局：隐藏/显示桌面图标（桌面整理快速触发） |
| `Space` | 详情页：播放/暂停动态壁纸 |
| `← →` | 详情页：上/下一张壁纸 |
| `Esc` | 详情页：返回壁纸库 |
| `Ctrl+F` | 主界面：聚焦搜索框 |

---

### F-13：壁纸播放调节（P2）— 对应 R-14

| 调节项 | 范围 | 默认 | 说明 |
|--------|------|------|------|
| 播放速度 | 0.5x / 1x / 1.5x / 2x | 1x | 仅视频壁纸 |
| 亮度 | -50% ~ +50% | 0 | 实时预览 |
| 饱和度 | -50% ~ +50% | 0 | 实时预览 |
| 对比度 | -50% ~ +50% | 0 | 实时预览 |

以上调节通过 GPU Shader / DirectX 滤镜实现，不修改源文件。

---

## 5. 交互规则

### 5.1 主界面导航变化（相比 V1）

```
┌──────────────────────────────────────────────────────────┐
│  [灵境]                          [设置] [最小化] [关闭]     │
├────────────────┬─────────────────────────────────────────┤
│                │                                         │
│  📝 AI 创作     │  ← V1 已有，无变化                        │
│  🖼️  我的壁纸   │  ← V2 升级为卡片缩略图网格，新增筛选/搜索/排序 │
│  🗂️  桌面整理   │  ← V2 新增（完整分区功能）               │
│  ⚙️  API 管理   │  ← V2 新增 DeepSeek / 可灵              │
│  📋  设置       │  ← V2 新增：定时轮换、退出恢复、多显示器     │
│                │                                         │
└────────────────┴─────────────────────────────────────────┘
```

### 5.2 壁纸库交互流程（V2 主要变化）

```
「我的壁纸」页面：

  [全部] [♥收藏] [✨AI生成] [本地] [动态]   [搜索...🔍]   [排序▼] [大/中/小]

  ┌────┐  ┌────┐  ┌────┐  ┌────┐
  │缩略图│  │缩略图│  │缩略图│  │缩略图│     ← 卡片网格（V2 升级）
  └────┘  └────┘  └────┘  └────┘
  
  点击卡片 → 进入详情页（F-02）
  hover 卡片 → 动态壁纸自动播放预览（无声）
  右键卡片 → 快捷菜单（设为壁纸 / 收藏 / 详情 / 导出 / 删除）
  长按卡片（触控）→ 进入多选模式
```

### 5.3 桌面整理交互流程（V2 新增）

```
「桌面整理」页面：

  左侧：分区列表（显示所有分区名称、图标数量、开关状态）
  右侧：桌面实时预览（透明覆盖层显示分区边界）

  操作栏：
    [+ 新建分区]  [文件夹门户]  [导入布局]  [导出布局]
    [隐藏所有图标]  [对齐网格]  [排序▼]

  分区右键菜单：
    [重命名]  [变色]  [透明度]  [卷起/展开]  [删除分区]

  Peek 功能：
    全局热键 Win+Shift+P → 临时显示所有分区（覆盖全屏应用）
```

### 5.4 关键状态补充（V2 新增）

| 状态 | 视觉表现 |
|------|---------|
| **缩略图生成中** | 卡片内显示骨架屏动画，后台生成完毕后刷新 |
| **正在轮换** | 系统托盘图标显示轮换动画；托盘右键菜单新增「下一张」「暂停轮换」 |
| **桌面整理模式** | 整个应用切换到「整理模式」视图，背景变暗，图标可拖拽 |
| **分区卷起状态** | 分区只显示标题栏，内容区折叠，桌面更清爽 |
| **退出确认** | （开关关闭时）直接恢复；（开关开启时）弹出："退出后将恢复原始桌面。[确认退出] [取消]" |
| **新版本可用** | 顶部横幅：「🎉 V2.1 已发布！查看更新内容 →」，可一键关闭 |

---

## 6. 范围定义

### 6.1 V2 功能范围总表

| 模块 | 功能 | 优先级 | 来源 |
|------|------|:--:|------|
| 壁纸库视觉化 | 卡片缩略图网格（含动态预览） | **P0** | R-01/02 |
| 壁纸库视觉化 | 缩略图自动生成（MP4/GIF/AI图/AI视频） | **P0** | R-01/02 |
| 壁纸详情页 | 全屏/侧边栏详情（大图+元数据+操作） | **P0** | R-03 |
| 壁纸详情页 | 动态壁纸详情内播放控制 | **P0** | R-03 |
| 壁纸排序 | 导入时间降序（默认）+ 多维度排序 | **P0** | R-04 |
| 退出恢复 | 退出时恢复原始桌面 + 崩溃恢复 | **P0** | R-05 |
| 桌面整理 | **完整分区功能（创建/命名/调色/卷起/图标拖入）** | **P0** | R-06 |
| 桌面整理 | 文件夹门户（P1）| P1 | R-06 扩展 |
| 桌面整理 | Peek 功能（P1）| P1 | R-06 扩展 |
| 桌面整理 | 自动整理规则（P1）| P1 | R-06 扩展 |
| 桌面整理 | 多显示器分区布局（P1）| P1 | R-06 扩展 |
| 应用内更新 | 版本检测 + 更新提示 | **P0** | R-16 |
| 壁纸收藏 | 收藏 + 标签 + 搜索 + 筛选 | P1 | R-09 |
| 多平台 API | DeepSeek（第一步）+ 可灵（第二步） | P1 | R-12 |
| 定时轮换 | 轮换列表 + 间隔 + 随机 | P1 | R-07 |
| 多显示器 | 每屏独立壁纸 | P1 | R-08 |
| 重新生成 | 基于历史 prompt 微调重新生成 | P1 | R-10 |
| 生成进度 | 分步进度条 + 预计剩余时间 + 取消 | P1 | R-13 |
| 快捷键 | 全局 + 详情页快捷键 | P2 | R-15 |
| 播放调节 | 速度/亮度/饱和度/对比度 | P2 | R-14 |
| 壁纸导出 | 壁纸库一键备份到指定目录 | P2 | R-11 |

### 6.2 不在 V2 范围

| 功能 | 原因 | 计划版本 |
|------|------|:--:|
| AI 实时壁纸演变 | 需要本地轻量模型，技术路线尚未确定 | V3.0 |
| 音乐可视化联动 | 独立音频分析管线，复杂度高 | V3.0 |
| 鼠标交互特效 | 独立技术栈 | V3.0 |
| UGC 社区 / 创意工坊 | 需要后端，V1 先验证市场 | V3.0 |
| macOS / Linux | 聚焦 Windows，先把 Windows 做好 | V4.0+ |
| 任务栏美化 | 系统耦合深，风险高 | 评估中 |
| 桌面小组件 | 超出壁纸工具范围 | 评估中 |

---

## 7. 数据与埋点（V2 新增）

### 7.1 新增埋点事件

| 事件 | 属性 | 目的 |
|------|------|------|
| `wallpaper_detail_open` | 壁纸 ID、类型（AI/本地）、来源（卡片点击/搜索） | 了解用户浏览习惯 |
| `wallpaper_detail_apply` | 从详情页设为壁纸 vs 从卡片设为壁纸 | 对比详情页转化效果 |
| `wallpaper_regenerate` | 从详情页重新生成次数、修改了哪些字段 | 优化 AI 生成满意度 |
| `desktop_organize_open` | 进入整理模式次数 | 整理功能使用率 |
| `partition_create` | 分区数量、命名、颜色选择 | 分区功能使用深度 |
| `partition_rollup_toggle` | 卷起/展开次数 | 卷起功能使用率 |
| `peek_trigger` | Peek 热键触发次数、场景（全屏/桌面） | Peek 功能价值评估 |
| `icon_hide_toggle` | 隐藏/显示图标操作次数 | 最常用的整理操作 |
| `wallpaper_rotate_enable` | 轮换间隔、来源（全部/收藏/标签） | 轮换功能配置偏好 |
| `exit_restore_trigger` | 恢复成功/失败、崩溃恢复 | 监控退出恢复可靠性 |
| `thumbnail_load_time` | 壁纸库打开到所有缩略图加载完毕耗时 | 性能监控 |

### 7.2 核心漏斗（V2 补充）

```
壁纸库浏览漏斗：
  打开壁纸库 → hover 预览 → 点击卡片（详情） → 设为壁纸
     100%         ?%            ?%               ?%

桌面整理漏斗：
  进入整理模式 → 创建分区 → 拖入图标 → 保存布局
      100%          ?%        ?%            ?%

分区功能漏斗：
  创建分区 → 命名 → 拖入图标 → 使用超过 1 次
    100%       ?%       ?%          ?%
```

---

## 8. 边界与异常（V2 新增）

| 场景 | 处理方案 |
|------|---------|
| **视频文件太大，缩略图提取慢** | 超过 500MB 的文件，缩略图提取后台进行，先显示「加载中」占位符 |
| **退出时原始壁纸文件已被删除** | 退出时检测快照文件存在性，不存在则直接退出（不恢复），记录日志 |
| **多显示器壁纸格式不兼容** | 如某格式在副屏不支持，提示用户选择兼容格式 |
| **桌面整理分区与壁纸冲突** | 分区透明覆盖层不影响壁纸播放，z-order 正确设置 |
| **取消生成时 API 已扣费** | 弹出提示：「已取消，Seedream 返回结果将被丢弃。注意：API 可能已扣除本次费用。」 |
| **Explorer 重启导致分区消失** | 检测到 Explorer 重启（窗口句柄失效），自动重新渲染分区覆盖层 |
| **快捷键与其他应用冲突** | 设置页允许用户自定义修改全局快捷键 |
| **文件夹门户映射的文件夹已被删除** | 分区内显示「文件夹不存在」，提示用户重新配置 |
| **100+ 桌面图标时分区渲染卡顿** | 分页加载图标缩略图，可见区域优先渲染 |

---

## 9. 设置补充（V2 新增选项）

| 分类 | 设置项 | 类型 | 默认值 |
|------|--------|------|--------|
| 壁纸库 | 默认视图（网格/列表） | 单选 | 网格 |
| 壁纸库 | 卡片尺寸（大/中/小） | 单选 | 中 |
| 壁纸库 | 默认排序（导入时间降序/最近使用…） | 下拉 | 导入时间降序 |
| 壁纸库 | hover 时自动播放动态预览 | 开关 | 开启 |
| 退出 | 退出时恢复原始桌面 | 开关 | **开启** |
| 退出 | 退出前询问确认 | 开关 | 关闭 |
| 桌面整理 | 启动时记忆上次整理状态 | 开关 | 开启 |
| 桌面整理 | 分区背景不透明度 | 滑块 | 20% |
| 桌面整理 | 分区标题栏不透明度 | 滑块 | 40% |
| 桌面整理 | Peek 热键 | 快捷键录入 | Win+Shift+P |
| 轮换 | 轮换列表 | 配置入口 | — |
| 多显示器 | 显示器壁纸配置 | 配置入口 | 同步所有屏幕 |
| 快捷键 | 自定义全局快捷键 | 配置入口 | — |

---

## 10. 优先级矩阵

| 优先级 | 功能 | 来源 |
|:--:|------|------|
| **P0** | 卡片缩略图网格（含动态预览 hover） | 用户诉求 R-01/02 |
| **P0** | 壁纸详情页（大图+元数据+操作） | 用户诉求 R-03 |
| **P0** | 导入时间降序排列（含多维度排序） | 用户诉求 R-04 |
| **P0** | 退出恢复原始桌面 + 崩溃恢复 | 用户诉求 R-05 |
| **P0** | **完整桌面分区功能（创建/命名/调色/卷起/图标拖入）** | 用户诉求 R-06 |
| **P0** | 应用内更新检测 | V1 遗漏 R-16 |
| **P1** | 壁纸收藏 + 标签 + 搜索筛选 | V1 遗漏 R-09 |
| **P1** | 多平台 API（DeepSeek + 可灵） | V1 遗漏 R-12 |
| **P1** | 生成进度实时反馈（分步进度条） | V1 遗漏 R-13 |
| **P1** | AI 历史重新生成（微调+重新生成） | V1 遗漏 R-10 |
| **P1** | 定时轮换 | V1.5 提前 R-07 |
| **P1** | 多显示器独立壁纸 | V1.5 提前 R-08 |
| **P1** | 桌面整理 —— 文件夹门户 + Peek + 自动规则 + 多屏 | 用户诉求 R-06 扩展 |
| **P2** | 快捷键支持 | V1 遗漏 R-15 |
| **P2** | 播放调节（亮度/速度/饱和度） | V1 遗漏 R-14 |
| **P2** | 壁纸导出/备份 | V1 遗漏 R-11 |

---

## 11. 开放问题

| # | 问题 | 影响 | 建议截止 |
|---|------|------|:--:|
| Q-V2-01 | 分区功能「文件夹门户」和「Peek」是否 V2 首发就做，还是 V2.1 再做？ | 开发排期 | V2 规划初期 |
| Q-V2-02 | 视频壁纸缩略图提取用 FFmpeg 还是 Windows Media Foundation？FFmpeg 功能强但包体增加约 40MB | 技术选型 | V2 开发前 |
| Q-V2-03 | 多显示器壁纸是否支持动态壁纸双屏同时播放？性能影响需要测试 | 性能风险 | V2 开发中 |
| Q-V2-04 | 取消生成时 Seedream API 是否仍然计费？需要调研火山引擎的计费策略 | 用户体验 / 商业 | V2 开发前 |
| Q-V2-05 | 可灵视频壁纸生成（$0.049/秒）成本较高，是否在 UI 上做醒目的成本预估提示？ | 用户信任 | V2 设计阶段 |
| Q-V2-06 | 分区「视觉覆盖式」实现中，如何精准获取每个图标的屏幕坐标？需要深入研究 SysListView32 消息 | 技术可行性 | V2 开发前 |

---

## 12. V2 产品价值总结

| 维度 | V1 | V2 升级 |
|------|----|----|
| **壁纸浏览** | 列表/文字为主，无图 | **全卡片缩略图，动态 hover 预览，详情页一览所有信息** |
| **壁纸管理** | 基础增删查 | **收藏/标签/搜索/排序，找壁纸效率大幅提升** |
| **退出体验** | 退出后壁纸残留 | **自动恢复原始桌面，干净退出，用户放心** |
| **桌面美化闭环** | 只管壁纸 | **壁纸 + 完整桌面分区整理，形成完整桌面美化体验** |
| **AI 生成体验** | 只有转圈动画 | **分步进度条 + 预估时间 + 可取消 + 历史可重新生成** |
| **平台生态** | 仅火山引擎 | **+ DeepSeek（第一步）+ 可灵（视频第二步）** |

---

## 13. 开发排期建议（V2 13 周）

| 周次 | 模块 | 关键交付 |
|:--:|------|---------|
| W1-W2 | F-01 壁纸库视觉化 | 卡片网格 + 缩略图生成 + 排序 |
| W3 | F-02 壁纸详情页 | 详情页 + 元数据 + 操作 |
| W4 | F-03 退出恢复 | 快照 + 恢复 + 崩溃恢复 |
| W5-W7 | **F-04 桌面整理（核心）** | **分区创建 + 命名 + 卷起 + 图标拖入** |
| W8 | F-04 桌面整理（高级） | 文件夹门户 + Peek + 自动规则（P1） |
| W9 | F-05 收藏标签 + F-06 多平台 API | 标签系统 + DeepSeek/可灵接入 |
| W10 | F-07 定时轮换 + F-08 多显示器 | 轮换引擎 + 多屏独立壁纸 |
| W11 | F-09 重新生成 + F-10 生成进度 | 历史重新生成 + 进度条 |
| W12 | F-11 更新检测 + F-12/F-13 快捷键/播放调节 | 更新检测 + 快捷键 + 播放调节 |
| W13 | 集成测试 + Steam 商店页准备 | 全功能测试 + 商店页素材 |

---

> **下一步**：V2 PRD 评审 → 确定 P0/P1/P2 最终边界 → 技术选型确认（分区实现路线 / 缩略图提取方案）→ 排期开发。
